# # utilities/data_upload.py
# from ..models import Property

# def upload_data_to_db(combined_data):
#     for record in combined_data:
#         Property.objects.update_or_create(defaults=record, id=record['id'])  # Assuming 'id' is the primary key
